# Changelog

## 未发布 — 菜单主题

- 曲目配置和设置的整块内容区与选中标签共用底色并无缝相连；标题与输入框采用适合浅色内容面的配色，配置标签固定在滚动内容上方。
- 曲目配置与设置分类统一为连续标签栏：选中项浅底深字、加粗、上圆角及青色底线，未选中项融入栏底；新增独立深浅主题色，避免选中状态反而更暗难辨。
- 深浅主题入口统一放入“设置 → 显示”；移除首页及采音侧栏的主题按钮。
- 修复原生画布 referencePixelsPerUnit=1 导致菜单九宫格圆角缩小 100 倍的问题；仅适配新增菜单图片，保留原画布及音符皮肤。

- 依据 PJSK 实机截图重构曲库、曲目配置、设置及新增采音侧栏，支持可记忆的深浅主题；保留原制谱器音符、轨道、工具图标和游玩／暂停／结算外观。
- 曲目配置页签、设置分类、独立多选混音、速度分段选择、高级功能折叠分组和 A/B 范围控件；PC 与窄横屏自适应。
- UPM 嵌入 UI Extensions 3.0.0 的固定提交及 Unity 6.6 兼容补丁，不改变项目 UGUI 版本。
- 修复从制谱器打开设置时无关按钮回调被清空的问题；主题与布局偏好使用独立键，不改变原有设置保存／取消语义。

## 未发布 — 制谱采音辅助

- 混音按钮用各声部对应波形的独立颜色高亮，移除对勾及面板顶部重复折叠入口。最多同时显示六条独立波形，可拖动各轨左侧分隔线调宽并记忆；调整时谱面同步让位，分隔线按下后高亮加粗。
- 修复右侧图标的悬停提示均变成“保存”：共用提示随图标更新本地化绑定，避免定期语言刷新覆盖；支持测试游玩、保存、采音、小地图的中日英名称。
- 制谱器右侧改为测试游玩、保存、采音、小地图四个图标入口，保留播放与精确时间；移除独立缩放／移动按钮。小地图支持独立折叠并记忆状态。
- 谱面以原生工具展开时的轨道宽度为上限，在左右侧栏之间居中或收窄；小地图按显示像素生成高分辨率纹理，长条连续插值，不再取整到整轨。
- 双指操作限定在谱面区域，按手指位移连续缩放；保留滚轮移动、Ctrl＋滚轮缩放及单指拖动。
- 左侧工具折叠入口改为淡紫／青色荧光小箭头：展开时贴工具列表右缘，折叠时回到屏幕左侧中央；移除“工具”文字按钮，保留独立触控热区和轨道自动扩展。
- 采音面板折叠后恢复原曲播放，不再受采音混音的声部选择、音乐静音或单轨音量影响；重新展开恢复原混音，切换不重置播放位置，也不改写已保存的混音设置。
- 制谱器加入可折叠的采音面板及独立工具折叠入口，沿用原有音符工具、撤销、保存和播放操作。
- 声部直接多选混音、最多六条对齐波形、实际起音提示、可移动草稿、A/B 循环，以及保音高慢放。
- 草稿落为正式单点/连续长条，编辑操作进入游戏撤销历史；支持节拍器、草稿提示音和音符音效分别开关。
- LRC / 增强 LRC 导入、逐句与逐音节试听、候选拍线和可撤销的 BPM 校准。
- Windows 随程序内置独立运行环境、Demucs 分轨及 MMS_FA 对齐权重，原曲自动离线分轨、缓存复用、真实进度与取消；移除解释器配置。构建逐文件校验引擎并随两种 Windows 发行包交付。低分对齐结果保留人工核对入口。
- 修复制谱器只有音符音效、原曲与分轨无声：为 CRI 编辑场景补齐 Unity 音频监听器，复用已有监听器，并随编辑器关闭停用。新增最终混音输出回归测试。
- 左侧工具栏折叠后，谱面自动向左扩展使用空出的空间；保留工具入口和小节编号间距，右侧采音面板位置稳定，重新展开及调整窗口大小时重算布局。
- 波形随缩放读取原始采样区间峰值，使用多级索引保留短促瞬态；按可见屏幕像素绘制，支持小于一个谱面 tick 的时间精度，避免放大后重复旧的 10 毫秒峰值缓存。

## 1.6.17 — 2026-09-09

### 社区功能与界面

- 在 Unity 6 分支合并 OpenSekai Community 的谱面编辑与游玩改进，包括自动保存、音频波形、备份恢复、自定义 Autoplay、判定线与引导线设置、负流速、单键变速和装饰音符等。
- 使用原始字体生成高质量动态 SDF 字形，改善 UI 字体锯齿。
- Windows 检测窗口 DPI 和渲染尺寸变化后自动重新采样字体，并保留原有排版度量，修复菜单说明文字消失。

### 视频生成

- 捕获最终合成画面，包含谱面、MV 和 UI，修正 Direct3D 画面上下翻转。
- 从 CRI 主输出采集音乐和音效；音频线程使用独立缓冲，避免渲染卡顿丢失音频采样，视频补帧保持实际录制时长。
- 编码进度和完成提示独立于场景，切换菜单后仍可见；失败保留素材，支持重新生成或重新保存。
- Windows 生成前检查程序目录、`ffmpeg/` 子目录和 `PATH` 中的 FFmpeg；缺失或无法运行时提前提示。
- Windows 使用 H.264/AAC 导出 MP4，正确处理编码进度、失败退出状态和音视频时长。
- Android 实现工作线程上的 MediaCodec H.264/AAC 编码与 MediaMuxer 合成，修正时间戳、色彩格式、轨道初始化及结束信号，无需 FFmpeg。
- Android 使用真实 MediaStore 结果确认相册保存，支持视频分享和保存失败重试；Android 10 及以上保存本程序的视频不再要求读取相册权限。
- 减少移动端音频校验和帧处理的内存、存储占用；生成期间保持屏幕唤醒，切到后台时中断并提示。

### 下载

| 产物 | 用途 |
| --- | --- |
| `win_amd64.zip` | Windows x64 便携版；视频导出需自行配置 FFmpeg。 |
| `win_amd64_ffmpeg.zip` | Windows x64 便携版；已包含 FFmpeg、运行依赖及许可说明。 |
| `arm64.apk` | Android ARM64 安装包，最低 Android 8.0（API 26）。 |

三个产物均使用 Unity `6000.6.0f1` 的非 Development 配置构建，内置版本为 `1.6.17`；Android versionCode 为 `10617`。源码仓库不包含构建目录、缓存、录制素材或临时测试文件。

### 验证与使用说明

- Windows 已通过可见窗口短视频验证，涵盖 UI 图层、画面方向、卡顿期间音频连续性、编码进度及完成提示。
- Android 已完成 ARM64 发行版构建及签名校验；本次未连接真机，设备硬件编码、相册和分享行为仍需真机验证。
- 请保持移动端应用在前台完成导出。旧录制中缺失的画面或声音无法通过重新编码补回，需使用新版重新录制。

### 菜单布局与实机回归（2026-09-10）

- 曲目配置和设置的完整内容区与选中页签共用底色，标签固定在滚动内容上方；修复隐藏设置页被普通按钮样式覆盖。
- 动态曲名显式使用文字绑定，避免本地化扫描替换成占位文案；调整标题、作者与难度摘要的间距。
- 窄横屏合并详情标题与操作行，保证至少可完整显示一行表单；配置保存按钮间增加留白。
- 短循环的 A/B 拖动手柄上下错开，保留原时间映射；波形分隔线补齐 CanvasRenderer，避免折叠与渲染异常。
- 图片／文字谱面工具使用不透明外层，避免背后操作图标透出；关闭按钮保持固定宽度，控件间增加间距。
- 更新页签、本地化、循环热区、波形分隔线回归测试；修正 TMP 已缓存字形被误报缺字的测试判定。

### 选项卡主题对比度修正（2026-09-10）

深色主题的选中页签／内容区改为偏紫高亮底色配浅字，浅色主题改为淡青底色配深蓝字，避免选中状态与内容背景融为一体。重新生成 Windows 构建并校验离线采音引擎。
