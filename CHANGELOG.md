# Changelog

## 1.6.17 — 2026-09-09

### 社区功能与界面

- 在 Unity 6 分支合并 OpenSekai Community 的谱面编辑与游玩改进，包括自动保存、音频波形、备份恢复、自定义 Autoplay、判定线与引导线设置、负流速、单键变速和装饰音符等。
- 使用原始字体生成高质量动态 SDF 字形，改善 UI 字体锯齿。
- Windows 检测窗口 DPI 和渲染尺寸变化后自动重新采样字体，并保留原有排版度量，修复菜单说明文字消失。

### 视频生成

- 捕获最终合成画面，包含谱面、MV 和 UI，修正 Direct3D 画面上下翻转。
- 从 CRI 主输出采集音乐和音效；音频线程使用独立缓冲，避免渲染卡顿丢失音频采样，视频补帧保持实际录制时长。
- 编码进度和完成提示独立于场景，切换菜单后仍可见；失败保留素材，支持重新生成或重新保存。
- Windows 生成前检查程序目录、`ffmpeg/` 子目录和 `PATH` 中的 FFmpeg；缺失或无法运行时提前提示。
- Windows 使用 H.264/AAC 导出 MP4，正确处理编码进度、失败退出状态和音视频时长。
- Android 实现工作线程上的 MediaCodec H.264/AAC 编码与 MediaMuxer 合成，修正时间戳、色彩格式、轨道初始化及结束信号，无需 FFmpeg。
- Android 使用真实 MediaStore 结果确认相册保存，支持视频分享和保存失败重试；Android 10 及以上保存本程序的视频不再要求读取相册权限。
- 减少移动端音频校验和帧处理的内存、存储占用；生成期间保持屏幕唤醒，切到后台时中断并提示。

### 下载

| 产物 | 用途 |
| --- | --- |
| `win_amd64.zip` | Windows x64 便携版；视频导出需自行配置 FFmpeg。 |
| `win_amd64_ffmpeg.zip` | Windows x64 便携版；已包含 FFmpeg、运行依赖及许可说明。 |
| `arm64.apk` | Android ARM64 安装包，最低 Android 8.0（API 26）。 |

三个产物均使用 Unity `6000.6.0f1` 的非 Development 配置构建，内置版本为 `1.6.17`；Android versionCode 为 `10617`。源码仓库不包含构建目录、缓存、录制素材或临时测试文件。

### 验证与使用说明

- Windows 已通过可见窗口短视频验证，涵盖 UI 图层、画面方向、卡顿期间音频连续性、编码进度及完成提示。
- Android 已完成 ARM64 发行版构建及签名校验；本次未连接真机，设备硬件编码、相册和分享行为仍需真机验证。
- 请保持移动端应用在前台完成导出。旧录制中缺失的画面或声音无法通过重新编码补回，需使用新版重新录制。
